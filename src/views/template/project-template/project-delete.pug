extends ../../base/base.pug

block content
    a(href=`/account/${loggedInUser._id}`) &larr; 뒤로가기
    form(method="POST")#project-upload__form
        label(for="title") 프로젝트명 : #{projectDB[0].name}
        input(name="name", placeholder="삭제할 프로젝트 이름", type="text", required)
        input(name="password", placeholder="비밀번호 확인", type="password", required)

        span 한 번 삭제한 프로젝트는 다시 생성할 수 없습니다. <br>
        span 신중하게 생각하고 삭제 절차를 진행해주세요. <br>

        input(name="create", value="프로젝트 제거 확인", type="submit")